tailwind.config = {
    theme: {
        extend: {
            colors: {
                accent: 'var(--accent)',
                surface: 'var(--surface)',
                textBase: 'var(--text-base)',
                bgBase: 'var(--bg-base)',
            },
            fontFamily: {
                display: 'var(--font-display)',
                body: 'var(--font-body)',
            }
        }
    }
}
